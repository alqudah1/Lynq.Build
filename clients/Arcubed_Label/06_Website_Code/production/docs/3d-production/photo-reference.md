# Photo Reference — moved

The photo → product mapping now lives at **`docs/photo-asset-map.md`**
(one level up) — it moved out of `3d-production/` because it's used by
more than the 3D pipeline (also the eventual real-photography import for
`product_images` and Ready for Delivery listings). Everything in this
package (`nova-spec.md`, `mini-luna-spec.md`, etc.) that references "photo
reference" means that file.

Kept as a redirect stub rather than deleted so old links don't break.
