{
  "name": "lynq-build",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "node --test test/*.test.js",
    "discover:leads": "node scripts/discover-leads.js"
  },
  "dependencies": {
    "nodemailer": "^9.0.5"
  }
}
